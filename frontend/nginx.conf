server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # API proxy: /api/* is forwarded to the Go backend with the /api prefix
    # stripped (the trailing slash on proxy_pass does the rewrite). This keeps
    # API paths from colliding with the SPA's client-side routes.
    location /api/ {
        proxy_pass http://backend:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Everything else is the Angular SPA: fall back to index.html so client-side
    # routes (e.g. /credit-analyses, /credit-analyses/:id) resolve on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
